<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.iot.platform.mapper.OperationLogMapper">

    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO operation_log (module, action, detail, operator)
        VALUES (#{module}, #{action}, #{detail}, #{operator})
    </insert>

    <select id="findPage" resultType="com.iot.platform.entity.OperationLog">
        SELECT id, module, action, detail, operator, create_time AS createTime
        FROM operation_log
        <where>
            <if test="module != null and module != ''">AND module = #{module}</if>
        </where>
        ORDER BY create_time DESC
    </select>
</mapper>
